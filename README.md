# kinya-dataset-ai
a repository with scripts &amp; worlflow on creating datasets for training of AI models relevant for Rwanda. It will consuder for examples  monolingual datasets, parallel text as well as relevant datasets in other languages (e.g. for domain specific training) 
